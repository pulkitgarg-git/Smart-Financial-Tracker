# Leave empty when using `npm run dev` (Vite proxies /api to the backend).
# For a split production deploy (Vercel UI + Render API), set the public API origin:
# VITE_API_BASE_URL=https://your-api.onrender.com
VITE_API_BASE_URL=